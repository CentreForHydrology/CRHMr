toHourly <-
function(CRHMdata){
  # converts time series to a shorter time series
  # uses linear interpolation for instantaneous values
  # precipitation data are distributed evenly
  
  
  # remove RH values as they are invalid when interpolated
  
}
